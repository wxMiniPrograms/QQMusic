<view>
  <swiper autoplay interval='{{interval}}' duaration='{{duaration}}' indicator-dots indicator-active-color='{{activeColor}}'>
    <block wx:for="{{swiperImg}}" wx:key="u">
      <swiper-item>
        <image src='{{item}}'></image>
      </swiper-item>
    </block>
  </swiper>
</view>